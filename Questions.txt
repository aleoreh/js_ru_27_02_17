Как правильно подключить к redux store свою собственную view-функцию/объект? Цель - добиться, чтобы при изменении redux state вызывалась эта функция и выполняла какие-то действия, например, изменение DOM.

Как правильно при старте приложения сразу вызвать какое-то action? Я делал так: после создания store с помощью функции createStore выполнял store.dispatch(myAction()). Стоит так делать, или есть более приемлемые способы?

Что происходит в middleware, когда мы возвращаем значение через return next(action), и просто вызываем next(action)? Что произойдёт, если ничего не возвратить?

Для того, чтобы подключить компонент к redux store, мы заворачиваем его в декоратор connect. Вторым аргументом мы передаём объект с action'ами, например, {dispatchMyAction: myAction}. Что происходит в connect'е такого, что мы теперь можем, вызвав dispatchMyAction, запустить обновление state?

Допустим, у нас есть готовый сайт, и мы хотим внедрить в него какую-то часть на react. Какие есть способы передачи данных и событий между react'ом и окружением? Например, что-то происходит во внешнем приложении, и нужно передать сообщение в react app, чтобы обновились его компоненты, и наоборот.